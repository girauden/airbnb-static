<ul class="list-inline text-center">
  <% ["adalberto", "mathilde", "denis"].each do |member| %>
    <li>
      <%= image_tag "#{member}.jpeg", class: "img-circle avatar-large" %>
      <h3><%= member.capitalize %></h3>
    </li>
  <% end %>
</ul>
